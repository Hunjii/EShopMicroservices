namespace Ordering.Infrastructure.Data.Constants
{
    internal static class TableNames
    {
        internal const string Roles = nameof(Roles);
        internal const string Permissions = nameof(Permissions);
    }
}
